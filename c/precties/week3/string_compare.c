#include <stdio.h>

int main()
{

    char a[100], b[100];
    scanf("%s %s", a, b);

    int i = 0;

    while ()
    {
        if (a[i] != '\0' && b[i] != '\0')
        {
            printf("Equal");
        }
        else if (a[i] == '\0')
        {
            printf("A is smaller");
        }
        else if (b[i] == '\0')
        {
            printf("B is smaller");
        }
        return 0;
    }